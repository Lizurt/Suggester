plugins {
    id 'java'
    id 'application'
    id 'org.openjfx.javafxplugin' version '0.0.10'
    id 'org.beryx.jlink' version '2.24.1'
    id 'antlr'
}

group 'com.example'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

ext {
    junitVersion = '5.9.0'
}

sourceCompatibility = "17.PREVIEW"
targetCompatibility = "17.PREVIEW"

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}

var antlrOutDir = "${project.buildDir}/generated-src/antlr/main/com/example/arangui/antlr"

generateGrammarSource {
    outputDirectory = file(antlrOutDir)
}

application {
    mainModule = 'com.example.arangui'
    mainClass = 'com.example.arangui.HelloApplication'
}

javafx {
    version = '17.0.1'
    modules = ['javafx.controls', 'javafx.fxml']
}

dependencies {
    antlr "org.antlr:antlr4:4.9.3"
    implementation 'com.github.curious-odd-man:rgxgen:1.4'
    implementation 'com.github.julianthome:inmemantlr-api:1.9.2'
    testRuntimeOnly("org.junit.jupiter:junit-jupiter-engine:${junitVersion}")
    testImplementation("org.junit.jupiter:junit-jupiter-api:${junitVersion}")
}

test {
    useJUnitPlatform()
}

jlink {
    imageZip = project.file("${buildDir}/distributions/app-${javafx.platform.classifier}.zip")
    options = ['--strip-debug', '--compress', '2', '--no-header-files', '--no-man-pages']
    launcher {
        name = 'app'
    }
}

jlinkZip {
    group = 'distribution'
}